# SECURITY（baseline 模板）

> 本文件是 **baseline 独立仓库**的 `SECURITY.md` 模板（v0）。  
> 拆仓后请复制/重命名为 baseline 仓库根目录的 `SECURITY.md`。

---

## 漏洞报告（Security issues）

请勿在公开 Issue 中披露可被立即利用的漏洞细节。

### 报告渠道

- 首选：GitHub Security Advisories（仓库启用后）  
- 备选：维护者邮箱（后续在 baseline 仓库公开）

### 报告内容建议

- 影响版本/commit（如可提供）
- 复现步骤（最小化）
- 预期与实际行为
- 影响范围评估（数据、权限、可用性）

---

## 响应与披露（建议口径）

- **确认**：收到后 \(48h\) 内确认是否进入处理
- **修复**：按严重程度分级排期
- **披露**：与季度 meta 披露机制对齐（不允许未披露的不对齐）

